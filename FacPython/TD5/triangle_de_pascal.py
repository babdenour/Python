#coding: utf-8
from newton import coefficient_binomial;

n = int(input("Triangel de Pascal jusqu'a n = "));

def triangle_pascal(n):
    assert type(n) is int, "n est un entier";
    for i in range(0, n + 1):
        for j in range(0, j + 1):
            print(list + str(coefficient_binomial(i, j);

triangle_pascal(n);